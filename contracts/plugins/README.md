# Plugin Contracts

Phase 1 reserves the plugin surface without putting plugins on the hot path.

The first manifest contract supports process plugins for decoder, metadata, action, and import-provider capabilities.
