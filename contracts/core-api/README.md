# Core API Contract

The Core API is shaped as a local HTTP API from the start. Windows desktop can bind it to localhost with a random session token; future Web/Docker can add authentication and mounted-root permissions without changing UI feature code.

Phase 1 only requires roots, folder/media browsing, thumbnails, preview, tasks, plugins, and file-operation commands.
